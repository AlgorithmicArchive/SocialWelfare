<div class="d-flex flex-column margin-center p-1 " style="min-height: 130vh;">
    <p class="font-heading mt-5 text-decoration-underline text-center" id="serviceName"></p>
    <button id="dummyData" class="btn btn-dark mx-auto mb-3">Dummy Data</button>
    <div class="container shadow-lg rounded p-5">
        <div class="container center-align sm-hidden">
            <p class="p-3 rounded bg-success text-white fw-bold">General Details</p>
            <p class="border border-primary lineNext h-0" style="width: 8vw;"></p>
            <p class="p-3 rounded bg-primary text-white fw-bold">Address Details</p>
            <p class="border border-primary lineNext h-0" style="width: 8vw;"></p>
            <p class="p-3 rounded bg-primary text-white fw-bold">Bank Details</p>
            <p class="border border-primary lineNext h-0" style="width: 8vw;"></p>
            <p class="p-3 rounded bg-primary text-white fw-bold">Documents</p>
        </div>
        <div id="formContainer">
            <form id="form1">
                <div class="container d-flex flex-column mt-5">
                    <div class="d-flex justify-content-center">
                        <img id="profile" src="/resources/profile.jpg" alt="Profile"
                            class="img-fluid rounded-circle object-fit-contain border border-2 border-dark"
                            style="width: 150px; height:150px;">
                    </div>
                    <div class="row">

                    </div>
                </div>
            </form>
            <form id="form2" style="display: none;">
                <div class="container d-flex flex-column mt-5">
                    <div class="row">

                    </div>
                </div>
            </form>
            <form id="form3" style="display: none;">
                <div class="container d-flex flex-column mt-5">
                    <div class="row">

                    </div>
                </div>
            </form>
            <form id="form4" style="display: none;">
                <div class="container d-flex flex-column mt-5">
                    <div class="row">

                    </div>
                </div>
            </form>
        </div>

        <div class="d-flex gap-2 justify-content-center mt-2">
            <button class="btn btn-dark width-25 mt-2" id="previous">Previous</button>
            <button class="btn btn-dark width-25 mt-2" id="next">Next</button>
            <button class="btn btn-dark width-25 mt-2" id="submit" style="display: none;">Submit</button>
        </div>
    </div>
</div>

<script>
    const application = @Html.Raw(Json.Serialize(Model));
    const generalForm = new FormData();
    const addressForm = new FormData();
    const bankForm = new FormData();
    const documentForm = new FormData();
    let ApplicationId = application.generalDetails == undefined ? null : application.generalDetails.applicationId;
    let PresentAddressId = application.preAddressDetails == undefined || application.preAddressDetails.length == 0 ? null : application.preAddressDetails[0].addressId;
    let PermanentAddressId = application.perAddressDetails == undefined || application.perAddressDetails.length == 0 ? null : application.perAddressDetails[0].addressId;
    let serviceContent = application.serviceContent;
    window.ApplicationId = ApplicationId;
    window.PresentAddressId = PresentAddressId;
    window.PermanentAddressId = PermanentAddressId;
</script>
<script src="/js/User/Forms/dummyData.js"></script>
<script src="/js/User/Forms/dataFetching.js"></script>
<script src="/js/User/Forms/formGeneration.js"></script>
<script src="/js/User/Forms/formHandling.js"></script>
<script src="/js/User/Forms/uiManupulations.js"></script>
<script src="/js/formvalidations.js"></script>
<script src="/js/User/Forms/serviceform.js"></script>